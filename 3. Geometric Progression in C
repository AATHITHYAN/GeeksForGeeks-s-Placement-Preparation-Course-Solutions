#include <stdio.h>
#include <math.h>

int Nth_of_GP(int a, int r, int n) 
{
   return( a * (int)(pow(r, n - 1)) );
}

int main() {
   
   int start = 1;
   
   int ratio = 2;
   
   int n = 8;
   
   printf("The %dth term of the series is: %d\n",n, Nth_of_GP(start, ratio, n) );
   
   return 0;
}
